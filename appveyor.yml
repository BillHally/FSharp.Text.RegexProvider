image: Visual Studio 2019 Preview
before_build:
  # Display .NET Core version
  - cmd: dotnet --version
init:
  - git config --global core.autocrlf input
build_script:
  - cmd: build.cmd
test: off
version: 0.0.1.{build}
artifacts:
  - path: bin
    name: bin
